* 결측치 제거 기준

-결측치 84% 이상 : 대체해도 신뢰 불가
-분산 = 0 : 전체가 동일한 값이라 학습 불가
-분산 < 0.0099 : 한쪽 값이 극히 적어 패턴 학습 불가
-상관계수 = 0 : 타겟과 관계 없음
난자기증나이 알수없음이라도 기증 여부로 변경해서 다른 파생변수로 사용하는게 좋을듯



* Data 변수 처리 방안

1. ID: 구분용이라 의미 없다 판단
2. 시술시기코드 + 시술 당시 나이 연관성 기대
3. 시술당시나이 
   1) 중앙값, 알수없음 전체 중앙값
   2) 34세 이하와 초과로 이진 분류 후 파생변수 활용
4. 시술유형
시술유형 (대분류)          특정시술유형 (세부 기법)
─────────────             ──────────────────────
IVF  250,060건    ───┬──► ICSI           122,368건
                     ├──► IVF             91,755건
                     ├──► Unknown         26,939건
                     ├──► ICSI:ICSI        2,314건
                     ├──► ICSI/BLASTOCYST  1,609건
                     ├──► IVF/BLASTOCYST   1,248건
                     ├──► IVF:IVF          1,146건
                     └──► ... (나머지 희귀 조합들)
                     
DI   6,291건      ───► (배아 관련 없음, NaN 처리)

5. 특정 시술 유형
    first_attempt = (총 임신 == 0) — 더직접적 신호
   long_infertility = (임신 경과 연수 >= 7) — Templeton 1996에서 유의  => 1~6은 어떻게 처리할지 결정필요!
    df['특정시술유형'].value_counts()  # 24개 전체 빈도
    df['특정시술유형'].nunique()        # 24 나와야 정상

실전 처리 코드 예시)
# 빈도 확인
counts = df['특정시술유형'].value_counts()
print(counts)

# 상위 N개만 남기고 나머지는 "기타"로 묶기
top_categories = counts.head(8).index
df['특정시술유형_grouped'] = df['특정시술유형'].where(
    df['특정시술유형'].isin(top_categories), 
    other='기타'
)

# 희귀 카테고리의 성공률이 특이한지 확인
df.groupby('특정시술유형').agg(
    건수=('임신성공', 'size'),
    성공률=('임신성공', 'mean')
).sort_values('건수', ascending=False)


전략 1: 단순 빈도 기반 (가장 쉬움)
pythontop = df['특정시술유형'].value_counts().head(8).index
df['시술유형_g'] = df['특정시술유형'].where(
    df['특정시술유형'].isin(top), '기타'
)
✅ 빠르고 간단
⚠️ 희귀하지만 의미 있는 신호를 잃을 수 있음

전략 2: 빈도 + 성공률 기반 (추천)
건수는 적어도 성공률이 극단적인 값은 살려두는 방식:
python# 100건 이상이거나, 성공률이 평균에서 크게 벗어나는 값은 유지
stats = df.groupby('특정시술유형')['임신성공'].agg(['size', 'mean'])
keep = stats[(stats['size'] >= 100) | 
             (abs(stats['mean'] - 0.25) > 0.1)].index
df['시술유형_g'] = df['특정시술유형'].where(
    df['특정시술유형'].isin(keep), '기타'
)
✅ 신호 보존하면서 노이즈 제거

전략 3: 피처 분해 (가장 강력)
아예 컬럼을 의미 단위로 쪼개는 방법. 아까 말한 거예요:
pythondf['주기법'] = df['특정시술유형'].str.extract(r'(IVF|ICSI|IUI)')[0]
df['배반포여부'] = df['특정시술유형'].str.contains('BLASTOCYST').astype(int)
df['반복시도'] = df['특정시술유형'].str.contains(':').astype(int)
df['조합시술'] = df['특정시술유형'].str.contains('/').astype(int)
✅ 24개 카테고리를 4개의 깔끔한 피처로 변환
✅ 모델이 각 요소의 영향을 독립적으로 학습
✅ 희귀 조합도 정보를 잃지 않음
결론
"15~16개를 기타로 묶기"는 좋은 출발점이에요. 다만:

먼저 희귀 값들의 성공률을 확인해서 중요한 신호가 있는지 보고
의미 있는 희귀 값(배반포 관련 등)은 살려두고
여유되면 전략 3처럼 피처 분해까지 해보세요

해커톤에서는 전략 2로 베이스라인 만들고, 시간 남으면 전략 3으로 성능 끌어올리는 순서가 효율적이에요.

6. 배란자극여부 : 0/1로 구분만
7. 배란 유도 유형
    0: 기록되지 않은 시행
    1: 알수없음(희귀값 포함)

8. 단일 배아 이식 여부
    Nan값을 3-2 나이 이진분류 값의 결과와 매칭
    eset_blast = (단일 이식 == 1) AND (blastocyst) — Kawwass 2013에서 최고 조합(OR 1.17 × 2.32)
    *intended_multi_actual_single = (단일==0) AND (이식==1) — 의도와 실제가 다른 "중간 탈락" 케이스
     => 막판 미세 점수 향상을 위한 필살기로 사용하는 전략!

9. 착상 전 유전 검사 사용 여부
   - 선별검사
   - 대상: 고령, 반복 유산, 반복 착상 실패 환자
   - 목적 : 성공률 높이기
1. 두 변수 교차표로 건수/성공률 확인 ← 먼저 이거!
   ↓
2. 건수 충분하고 성공률 차이 뚜렷 → 계속 진행
   건수 적거나 차이 미미 → 포기하고 다른 조합 탐색
   ↓
3. 단순 AND 변수 만들어서 CV 점수 측정
   ↓
4. 개선 크면 → 옵션 2, 3의 더 정교한 버전 시도
   개선 미미 → 다른 피처로 전환
   ↓
5. 최종적으로 채택한 피처는 발표자료에 스토리로 녹이기   

10. 착상 전 유전 진단 사용 여부
    - 진단검사
    - 대상: 유전병 가족력이 있는 부부
    - 목적: 건강한 아이 출산

11~17. 불임원인 범주형 17개
    - 

18~27. 불임 원인 Boolean 세부 10개

원본 7개 그대로 유지
   +
집계 피처 3~5개 (전체 원인 수, 양측 여부, 각 주체 플래그)
   +
검증된 상호작용 2~3개 (남성원인×ICSI 등)
   +
(옵션) 종합 프로파일 Target Encoding 1개

=> 이렇게 해서 총 15~20개의 파생변수를 만들고, 모델이 그 중 유용한 걸 고르게 하세요.
"합치고 싶은 욕심"은 좋은 직감이에요. 그 직감을 여러 각도의 파생변수로 표현하는 게 실전 피처 엔지니어링이에요. 한 방에 해결하려 하지 말고, 여러 번의 작은 시도로 쌓아가세요!

from sklearn.model_selection import cross_val_score
from lightgbm import LGBMClassifier

def evaluate(X, y, name):
    model = LGBMClassifier(random_state=42, verbose=-1)
    score = cross_val_score(model, X, y, cv=5, scoring='roc_auc').mean()
    print(f"{name}: AUC = {score:.4f}")
    return score

# 실험 A: 원본 7개만
X_a = df[cause_cols_7]
score_a = evaluate(X_a, y, "원본만")

# 실험 B: 원본 + 집계 피처
X_b = df[cause_cols_7 + ['전체_원인_수', '양측_원인', '남성_관련']]
score_b = evaluate(X_b, y, "원본 + 집계")

# 실험 C: 원본 + 집계 + 시술 상호작용
X_c = df[cause_cols_7 + ['전체_원인_수', '양측_원인', '남성원인_ICSI_궁합']]
score_c = evaluate(X_c, y, "원본 + 집계 + 상호작용")

# 실험 D: 원본 + 종합 프로파일 (One-Hot)
X_d = pd.concat([df[cause_cols_7], pd.get_dummies(df['종합_프로파일'])], axis=1)
score_d = evaluate(X_d, y, "원본 + 종합(OneHot)")

# 실험 E: 종합 프로파일 Target Encoding
X_e = df[cause_cols_7 + ['종합_프로파일_TE']]
score_e = evaluate(X_e, y, "원본 + 종합(TE)")

28. 배아 생성 주요 이유
# DI 케이스 (6,291건)
is_현재시술용 = 0
is_기증용    = 0
is_배아저장용 = 0
is_난자저장용 = 0
is_연구용    = 0
# → 5개 다 0 → "배아 관련 활동 없음"을 자연스럽게 표현

# IVF 표준 케이스 (233,732건, "현재 시술용")
is_현재시술용 = 1
is_기증용    = 0
is_배아저장용 = 0
is_난자저장용 = 0
is_연구용    = 0
# → is_현재시술용만 1

# 복합 케이스 (3,784건, "기증용, 현재 시술용")
is_현재시술용 = 1
is_기증용    = 1
is_배아저장용 = 0
is_난자저장용 = 0
is_연구용    = 0
# → 2개 플래그 동시 활성화

29~38.

[우선순위 1] 기본 전처리 (필수)
├─ 문자열 → 숫자
└─ is_censored 플래그 생성

[우선순위 2] Templeton 기반 플래그 (강력)
├─ is_first_attempt
├─ repeated_3plus  
├─ ever_delivered ⭐
└─ prev_birth_rate

[우선순위 3] 전환율 피처 (차별화)
├─ 임신_전환율
├─ 출산_유지율
├─ 누적_성공률
└─ 유산_횟수

[우선순위 4] 종합 스코어 (상위권)
├─ 예후_점수
├─ 시술_경험_레벨
└─ 다른 컬럼과의 상호작용

[우선순위 5] 도메인 지식 추가 (발표용)
└─ 논문 인용과 함께 스토리텔링



39. 총 생성 배아 수
   - 파생변수 추천:
• 배아_있음 = (총 생성 배아 수 > 0) — 기본 플래그
• 배아_풍부 = (총 생성 배아 수 >= 5) — 양질의 사이클
• 배아_생성률 = 총 생성 배아 수 / (수집된 난자 수 + 1) — 효율성

추천 그룹범위임상적 의미성공률
저반응0~6개난소 반응 저하, 고령 다수22%
정상반응7~14개이상적 영역, 성공률 최고35~36%
고반응15~20개OHSS 경계, freeze-all 등장33%
과반응21개+PCOS 의심, 전부 동결 전략23%


40. 미세주입된 난자 수
    - 0인 경우 / 1이상인 경우 그룹별로

41. 미세주입에서 생성된 배아 수


42. 이식된 배아 수


43. 미세주입 배아 이식 수


44. 저장된 배아 수


45. 미세주입 후 저장된 배아 수


46. 해동된 배아 수



47. 해동 난자 수


48. 수집된 신선 난자 후


49. 저장된 신선 난자 수



50. 혼합된 난자 수


51. 파트너 정자와 혼합된 난자 수


52. 기증자 정자와 혼합된 난자 수


53. 난자 출처


54. 정자 출처


55. 난자 기증자 나이


56. 정자 기증자 나이


57. 동결 배아 사용 여부


58. 신선 배아 사용 여부


59. 기증 배아 사용 ㅕ부


60. 대리모 여부


61. PGD 시술 여부


62. 



