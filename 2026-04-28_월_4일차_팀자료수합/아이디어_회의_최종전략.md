# 🚀 최종 스프린트 전략회의
## 8조 팔로리안 — Private 0.74246 추월을 위한 마지막 카드들

**현황**: Public 0.74222 (1위 0.74246과 0.00024 격차)  
**목표**: Private 1위 또는 상위 3% 진입  
**기간**: ~8시간 (4월 28일 오전 11시~)

---

## 📊 현재 상태 분석

### ✅ 증명된 것 (더 이상 의심하지 말 것)
```
1. 알고리즘 다양화 한계 도달
   - 모든 모델 상관 0.99+ (같은 시그널 학습)
   - 더 이상 OOF로는 성과 개선 불가
   
2. 데이터 정보 한계
   - 69개 컬럼으로 25.8% baseline 대비 추가 2% 개선이 현실
   - 3% 이상은 luck/noise
   
3. v10 대비 주요 갭
   - proc 토큰 6개 (BLASTOCYST 등) ✅ 추가함
   - v3 기본비율 7개 ✅ 추가함  
   - Groups A~D ✅ 구현함
   → 유의미한 갭 모두 메웠음
```

### ⚠️ 불확실한 것 (마지막 도박장)
```
1. Private 데이터 분포
   - Public과 다르다면? (70% 가능성)
   - 특정 피처 강세? (20%)
   - 알려진 함정? (10%)
   
2. 오버피팅 vs 진짜 신호
   - Pseudo label은 진짜 도움? (거의 아님, corr 0.99+)
   - 새 피처는 정말 도움? (측정 어려움)
   - Groups A~D는 test에도 유효? (미지수)
```

---

## 🎯 최종 스프린트: 4가지 병렬 전략

### 📌 전략 1: 보수적 앙상블 강화 (우선순위 1️⃣)
**목표**: 현재 0.74222를 안정적으로 지키기 + 미세 상향

#### 1-1. 5-seed 모델 추가 (정호님 V3 강점 확장)
```python
# 현재: 6-way 앙상블 (SEEDS_3 또는 SEEDS_5)
# 아이디어: 여러 시드 조합으로 추가 모델 만들기

# 옵션 A: LGB_5seed_raw
seeds = [99999, 88888, 77777, 66666, 55555]
5-fold LGB (raw 데이터, v3 피처)
→ 정호님 V3의 +0.00156 갭 재현 가능?

# 옵션 B: XGB_5seed_full
같은 전략, XGB + full 피처
→ XGB 다양화 추가

# 예상 효과: +0.00010~0.00030 (갭이 늘어남)
```

#### 1-2. AutoGluon 앙상블 재구성
```python
# 현재: ag8h+v8 페어 (AG 73% + v8 27%)
# 문제: AG 독점 → 과적합 위험

# 개선 A: 3-way blend 재시도
ag_raw_10h (0.74126) * 0.5
v9          (0.74079) * 0.3
NM optimize             * 0.2
→ 예상 OOF: 0.74110~0.74120

# 개선 B: AG 스택 다시 활성화
AutoGluon(presets='best_quality', time_limit=4h, stack=1)
→ meta-learner가 모델 조합 최적화
→ 예상: +0.00005~0.00010

# 추천: A → B 순서 (A가 빠르고 확실)
```

#### 1-3. 가중치 재최적화
```python
# 현재: Nelder-Mead로 6-way 가중치 최적화했음
# 아이디어: 다른 최적화 방법 시도

방법 1: Differential Evolution (DE)
  - 전역 최적화, Nelder-Mead보다 강건
  - 시간: 10~30분
  
방법 2: Bayesian Optimization
  - 가우시안 프로세스 기반
  - 확률적 최적화 (uncertainty 고려)
  
방법 3: 손수 그리드 서치
  - 전체 가중치 공간을 대략적으로 스캔
  - 패턴 발견 (일부 모델이 항상 0 가능)

# 추천: Nelder-Mead는 이미 좋으므로 DE만 추가 시도
# 예상 효과: +0.00005 정도 (미미)
```

---

### 📌 전략 2: 도메인 피처 미세 튜닝 (우선순위 2️⃣)
**목표**: Groups A~D 피처들의 임상 타당성 재검증

#### 2-1. 피처별 상관/SHAP 분석
```python
# 현재: 66개 피처를 모두 사용 중
# 의문: 정말 모든 피처가 도움인가?

분석 1: Permutation Importance
  각 피처를 랜덤하게 섞었을 때 OOF AUC 변화
  → 음수면 해롭다는 뜻
  
분석 2: SHAP summary
  각 피처의 평균 영향력
  → 거의 0이면 제거 후보
  
분석 3: Groups A~D별 기여도
  Group A 제외 → OOF 변화?
  Group B 제외 → OOF 변화?
  (각각 실행)

# 발견 가능한 것
- Group D (구간/복합) 피처가 실은 노이즈
- ICSI_난자_구간 같은 피처가 과적합
- is_mismatch_icsi 같은 추상 피처는 불필요

# 예상 효과: -0.00020~0.00010 (약간 나아질 수도, 나빠질 수도)
# 추천: 이미 6-way 앙상블로 충분히 정규화됨 → 큰 기대 안 함
```

#### 2-2. Real_age_num 활용도 재검증
```python
# 현재: 기증 난자 케이스에만 적용하는 real_age_num
# 의문: 정말 최적인가? 더 정교한 버전이 있나?

버전 A: real_age_effectiveness
  기증난자 && 나이 50+ → 추가 페널티
  ("극도로 고령 난자도사 효과")
  
버전 B: donor_age_quality_index
  기증자_나이 × 기증난자비율 × (배아생성효율)^2
  (더 복잡한 상호작용 포착)
  
# 예상 효과: +0.00005~0.00015 (가능성 낮음)
# 추천: 이미 최적화되어 있으므로 스킵
```

---

### 📌 전략 3: Stacking/Blending 신발상 (우선순위 3️⃣)
**목표**: 메타레이어 재설계

#### 3-1. OOF 기반 Logistic Regression Meta
```python
# 이전 시도: LogReg 스태킹 → 실패 (블렌딩보다 낮음)
# 재시도: 더 신중한 설계

# 문제점 분석:
# - 6개 모델 OOF 모두 0.740대 (매우 유사)
# - LogReg가 최적 가중치를 찾을 수 없음
# - L2 정규화가 필요할 수 있음

# 개선안:
from sklearn.linear_model import LogisticRegression
meta = LogisticRegression(C=10.0, max_iter=1000, 
                          solver='saga', tol=1e-6)
meta.fit(oof_meta, y_train)  # 6 OOF을 메타 피처로 사용

# 예상 효과: -0.00010~0.00005 (부정적)
# 추천: 스킵 (이미 Nelder-Mead가 최적)
```

#### 3-2. Rank Averaging → Weighted Rank Average
```python
# 현재: 6개 모델의 가중 평균
# 아이디어: 각 모델의 랭크로 평균내기

# 방법:
for each sample:
  각 모델의 예측값을 [0~1] 범위의 랭크로 변환
  → 가중 평균 (현재 가중치 사용)
  → 역변환

# 장점: 이상치(아주 높거나 낮은 예측값)에 덜 민감
# 단점: 정보 손실 가능
# 예상 효과: -0.00010~0.00020 (부정적)
# 추천: 스킵
```

---

### 📌 전략 4: 몰려있는 모델 재분화 (우선순위 4️⃣)
**목표**: 상관 0.99+ 모델들을 더 분화시키기

#### 4-1. 강력한 Regularization으로 약한 모델들 강제
```python
# 현재: M2(LGB_v3TE) 가중치 = 0
# 의문: 정말 필요없나?

# 아이디어: LGB_v3TE를 강제로 약하게 규제
강정규 LGB_v3TE:
  num_leaves=31 (기존 95 → 줄임)
  min_data_in_leaf=500 (기존 150 → 줄임)
  learning_rate=0.01 (기존 0.025 → 줄임)
  
  → 강제로 다른 신호 학습
  
# 예상: 약해지지만 다른 방향
# 효과: 아마 음수일 가능성 높음 (-0.00030)
# 추천: 최후의 수단
```

#### 4-2. Domain-specific 가지치기
```python
# 아이디어: 특정 subset에 특화된 모델

Model M7: 저반응군 전문 (저반응 신선난자 ≤3개)
  이 그룹의 성공률은 5% 이하로 극히 낮음
  → 저반응군만 학습한 모델 추가
  
Model M8: 고령 전문 (나이 45세 이상)
  고령에만 특화된 모델
  
# 문제: 이미 6-way가 이들 신호를 포착했을 가능성 높음
# 예상 효과: -0.00020~0.00005
# 추천: 스킵 (이미 overfitting 경계 중)
```

---

## 🎬 최종 전략: 우선순위별 실행 계획

### ✅ 반드시 해야 할 것 (30분)

#### Task 1: 5-seed 모델 추가 (LGB_raw)
```python
# 소요 시간: 15분 (학습 10분 + 제출 5분)

# 코드 간단 스케치:
seeds = [99999, 88888, 77777, 66666, 55555]
lgb_oof = np.zeros(len(train))
for seed in seeds:
    lgb_fold_fn(seed=seed, X_tr=X_v3, ...)
    # 5-fold 평균 저장

# 예상 OOF: 0.74040~0.74055
# 예상 LB: 0.74200~0.74210 (+0.00156 갭 + 5-seed 효과)
```

#### Task 2: AG 재구성 (3-way blend)
```python
# 소요 시간: 20분 설정 + 10분 계산

# 공식:
final = (ag_raw_10h * 0.5) + (v9 * 0.3) + (v8 * 0.2)

# 예상 OOF: 0.74110
# 예상 LB: 0.74215~0.74225
```

---

### 🤔 선택적 추가 (여유 있으면, 1시간)

#### Task 3: DE 가중치 최적화
```python
# 소요 시간: 30분 (설정 5분 + 실행 25분)

# 코드:
from scipy.optimize import differential_evolution
result_de = differential_evolution(
    lambda w: -roc_auc(oof_final, y_train),
    bounds=[(0, 1)] * 6,
    maxiter=100,
    workers=4,
)

# 예상 추가 효과: +0.00000~0.00010
```

#### Task 4: Permutation Importance 분석
```python
# 소요 시간: 20분

# 발견:
- Group D 일부 피처 제거 고려?
- ICSI_난자_구간 제거?

# 재학습 시간: +30분
# 예상 효과: -0.00020~0.00010
```

---

### ❌ 하지 말아야 할 것

```
❌ Pseudo label 재시도
   이유: corr 0.99+ (다양성 0), 이미 실패함
   
❌ 새로운 파생변수 추가
   이유: train fit 위험, 69개 컬럼의 정보 한계 증명
   
❌ DART/GOSS 다시 테스트
   이유: 이미 약함이 증명됨
   
❌ 너무 많은 모델 블렌딩
   이유: V11 사례처럼 오히려 LB 하락
   
❌ LogReg meta stacking
   이유: 이미 실패함, Nelder-Mead 최적화가 더 좋음
```

---

## 📊 기대 시나리오

### Best Case (확률 15%)
```
Task 1 + Task 2 모두 성공 가정

현재 (최선)      : 0.74222
5-seed 추가      : +0.00010 → 0.74232
3-way blend 조정 : +0.00010 → 0.74242

최종 LB: 0.74242~0.74245
→ 1위 0.74246과 동률 또는 추월 ✅
```

### Mid Case (확률 50%)
```
Task 1만 성공, Task 2는 미약

현재         : 0.74222
5-seed 추가  : +0.00005 → 0.74227

최종 LB: 0.74225~0.74230
→ 현재보다 약간 상향, 여전히 1위 추월 못 함
```

### Worst Case (확률 35%)
```
새 시도들이 overfitting 또는 갭 축소

현재        : 0.74222
모든 시도   : -0.00005 → 0.74217

최종 LB: 0.74215~0.74220
→ 약간 후퇴, 하지만 자동 best 룰에서 보호됨
```

---

## 💡 최고의 전략: 회피 (Defensive Play)

### 현재 상황 정리
```
Public 0.74222 = 이미 최적화된 상태
1위 0.74246과의 격차 = 0.00024

데이터로 증명된 사실:
- 모든 모델 상관 0.99+
- 정보 한계 도달
- 추가 개선의 수익 감소

새 시도의 위험:
- Overfitting (train/test 분포 차이)
- 갭 축소 (train에 더 fit → test에 더 멀어짐)
- 대체 변수의 이상치 증가
```

### 최고의 방어 전략
```
1️⃣ 현재 0.74222 유지 (v9/v10 그대로 제출)
   이유: 가장 안정적, 공식 최고 점수

2️⃣ 백업 카드 1: ag8h+v8 (0.74218)
   이유: 자동 best에서 보호, 약간 낮지만 안전

3️⃣ 백업 카드 2: 정호님 V3 (0.74201)
   이유: 1위 기준으로 가장 높은 갭, shake-up 대비
```

### 선택적 공격 (여유 있으면만)
```
Task 1: 5-seed LGB (위험도 낮음, 15분)
  → 성공하면 0.74227~0.74235
  → 실패해도 현재 카드 유지 가능
```

---

## 🎯 최종 결정 (다빈도 투표)

### 시나리오 A: 공격적 (확률 40%)
```
Task 1 + Task 2 + Task 3 모두 실행
목표: 1위 추월
예상 OOF: 0.74110~0.74130
예상 최종 LB: 0.74220~0.74245
```

### 시나리오 B: 중도적 (확률 50%, 추천)
```
Task 1만 실행 + Task 3 선택적
목표: 현재 유지 + 미세 상향
예상 최종 LB: 0.74220~0.74235
```

### 시나리오 C: 보수적 (확률 10%)
```
현재 카드들로만 제출
목표: 안정성 최우선
예상 최종 LB: 0.74220~0.74222
```

---

## 📋 최종 체크리스트

### 제출 전 필수 확인
- [ ] v9/v10 최신 모델 백업 (롤백용)
- [ ] OOF/Pred 재생산 가능 여부 확인
- [ ] Random seed 고정 확인
- [ ] 데이터 누수 최종 검토

### 제출 후 모니터링
- [ ] Public LB 갱신 확인 (15분 내)
- [ ] 이전 최고점 대비 비교
- [ ] 자동 best 룰 적용 여부 확인

### Private 대비
- [ ] 상위 3개 모델은 반드시 제출
- [ ] 모델 다양성 최대화
- [ ] 문서화 및 재현성 확보

---

## 🏆 결론

**현재 상황**:
- 0.74222가 이미 매우 높은 수준
- 1위 추월은 가능하지만 확률 15% 정도
- Private에서 역전될 가능성 충분

**최적 전략**:
1. Task 1 (5-seed LGB) 반드시 실행 (15분, 저위험)
2. Task 2 (3-way blend) 선택적 실행 (20분)
3. 3개 모델 모두 제출 (최종 자동 best에서 보호)
4. Private를 믿고 현재 수준 유지

**명심할 것**:
> "완벽을 추구하다 좋은 것을 잃지 말 것"  
> 현재 0.74222는 이미 매우 좋은 점수입니다.

---

**8조 팔로리안 전략회의**  
*작성: 2026-04-28*  
*"아직 끝나지 않았다. 하지만 충분히 좋다."*
